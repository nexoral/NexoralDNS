{
  "name": "nexoraldns-workspace-root",
  "private": true,
  "description": "Workspace root linking server/, shared/, and DHCP/ so common third-party dependencies resolve to a single installed copy. Web/ is a Go module and is built separately; tools/ and client/ are intentionally NOT members.",
  "workspaces": [
    "server",
    "shared",
    "DHCP"
  ]
}
